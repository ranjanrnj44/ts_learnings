"use strict";
//instead of repeating our code for n times we can make use of types
//repeated
let add = (a, b) => {
    return a + b;
};
console.log(add(1, 2));
function sub(x, y) {
    return x - y;
}
console.log(9 - 3);
let mul = (p, q) => p * q;
mul(1, 6);
let div = (d, f) => d / f;
div(4, 2);
let multiply = (p, q) => p * q;
mul(1, 6);
let divide = (d, f) => d / f;
div(4, 2);
